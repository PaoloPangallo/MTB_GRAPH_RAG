# Reviewer instructions G1.1

Review only active-claim units in the assigned blind packet. Do not load the
excluded-unit files. Record all schema fields, keep `bucket=null` when
`evaluable=false`, and distinguish source/provenance limitations from
applicability. Never infer or inspect system predictions, ranks, scores,
reason codes, gates, run IDs or original result position.
