# Missing-source policy

A missing locator or unavailable source is recorded in `source_available` and
`provenance_status`. It is not automatically converted to `rejected` and does
not erase the applicability dimensions. Reviewers mark the claim as
non-evaluable only when the missing source prevents the protocol-required
assessment; otherwise they assign the applicable bucket with the limitation
recorded.
