# Unseen-document validation

Independent pair: `GCA-0101aa9c8f708d6f8dd74be0`, provenance `pmid:24088390`, resolved automatically to `pmcid:PMC4157820`.

In an empty temporary runtime cache: API fetch succeeded, the canonical JATS parser produced 236 SourceUnits, the frozen selector produced five IDs, Gemma V2 returned `ABSTAIN`, the validator returned `ENRICHMENT_V2_ABSTAINED`, and the unchanged orchestrator reached `COMPLETED` when the unchanged narrator was enabled.

No manual identifier was supplied and no frozen bundle source-unit IDs were used.
