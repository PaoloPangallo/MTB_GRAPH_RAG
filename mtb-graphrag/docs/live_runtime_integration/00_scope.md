# LIVE document retrieval and selector integration

This phase promotes already evaluated components into LIVE execution. It does not redesign the selector, Gemma V2, quote validation, GCA semantics, canonical gates, dossier, or narrator.

LIVE performs fresh deterministic execution with cache-first authorized document resolution and API-on-miss. REPLAY remains an exact frozen-artifact path.

The promoted selector is frozen at `deterministic-sourceunit-selector/1.0`, with runtime top-k `5`.
