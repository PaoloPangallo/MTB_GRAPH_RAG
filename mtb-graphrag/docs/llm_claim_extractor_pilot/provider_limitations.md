# Limiti del provider

MiniMax M3 cloud accetta la tool definition flat, ma continua a restituire un campo `thinking` non vuoto nonostante `think=false` sia richiesto. Questo campo resta separato e non viene validato.

Il trasporto flat ? risultato valido, ma il pilot semantico non ha prodotto status finali: le quote sono risultate ambigue o non sufficienti e una direction ? stata rifiutata. Non ? quindi autorizzata l?esecuzione delle 75 chiamate.
