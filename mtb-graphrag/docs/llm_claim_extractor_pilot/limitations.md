# Limiti

Il modello cloud ha risposto con tool call, ma con argomenti non conformi in tutti i tre casi. Non si pu? stimare stabilit? semantica n? eseguire il pilot completo. Il primo smoke non consente una diagnosi retrospettiva pi? precisa perch? il raw runner non aveva conservato il contenuto. Il test corretto termina prima di `ClaimSupportVerifier`; non produce claim, gate, score o bucket.

La cache raw resta locale e ignorata da Git. I report tracciati contengono soltanto metadati, hash e diagnostica strutturale.
