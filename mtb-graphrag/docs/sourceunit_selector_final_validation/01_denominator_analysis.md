# Denominator analysis

The frozen gold contains 20 candidate-document cases: 9 positive cases with at least one `DIRECTLY_RELEVANT` unit and 11 zero-direct cases. Selector HitRate@5 on positive cases is 9/9 = 1.0000. The earlier aggregate denominator of 20 therefore mixed retrievable evidence cases with cases where the primary gold contains no direct evidence.
