# Dipendenze di runtime del backend.
#
# Le versioni sono vincolate al minore installato e verificato: un elenco senza
# versioni non descrive l'ambiente in cui gli esperimenti sono stati eseguiti, e
# un revisore che lo installasse oggi otterrebbe un ambiente diverso.
#
# L'ambiente esatto di questa fase e' congelato in requirements-lock.txt.
# Le dipendenze necessarie a eseguire test ed evaluation sono in
# requirements-dev.txt: `pip install -r requirements.txt` da solo NON basta a
# far girare le suite.

fastapi>=0.136,<1.0
uvicorn[standard]>=0.49,<1.0
langgraph>=1.2,<2.0
langchain-core>=1.4,<2.0
langchain-ollama>=1.1,<2.0
neo4j>=6.2,<7.0
python-dotenv>=1.2,<2.0
requests>=2.34,<3.0
pydantic>=2.13,<3.0
typing-extensions>=4.12
